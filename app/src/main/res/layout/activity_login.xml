<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
        xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:tools="http://schemas.android.com/tools"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@android:color/holo_blue_light"
        tools:context=".registerlogin.LoginActivity">

    <EditText
            android:layout_width="0dp"
            android:layout_height="50dp"
            android:inputType="textEmailAddress"
            android:ems="10"
            android:paddingLeft="16dp"
            android:background="@drawable/rounded_edit_text_register_login"
            android:hint="Email"
            android:id="@+id/email_login_edit_text" app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintStart_toStartOf="parent" app:layout_constraintEnd_toEndOf="parent"
            android:layout_marginTop="32dp" android:layout_marginLeft="32dp" android:layout_marginStart="32dp"
            android:layout_marginRight="32dp" android:layout_marginEnd="32dp"/>
    <EditText
            android:layout_width="0dp"
            android:layout_height="50dp"
            android:inputType="textPassword"
            android:ems="10"
            android:paddingLeft="16dp"
            android:background="@drawable/rounded_edit_text_register_login"
            android:hint="Password"
            android:id="@+id/password_login_edit_text"
            app:layout_constraintTop_toBottomOf="@+id/email_login_edit_text"
            app:layout_constraintStart_toStartOf="@+id/email_login_edit_text"
            app:layout_constraintEnd_toEndOf="@+id/email_login_edit_text" android:layout_marginTop="8dp"/>

    <Button
            android:text="@string/login"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:id="@+id/login_button" app:layout_constraintStart_toStartOf="@+id/password_login_edit_text"
            app:layout_constraintEnd_toEndOf="@+id/password_login_edit_text"
            android:background="@drawable/rounded_button_register_login"
            app:layout_constraintTop_toBottomOf="@+id/password_login_edit_text" android:layout_marginTop="8dp"/>
</androidx.constraintlayout.widget.ConstraintLayout>